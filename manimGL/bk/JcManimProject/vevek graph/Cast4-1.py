from big_ol_pile_of_manim_imports import *

class NumberLineTest(Scene):
    def construct(self):
        n=NumberLine()
        self.add(n)
        n=NumberLine(
        x_min=-3,
        x_min=3,
        include
        )
        self.add(n)
